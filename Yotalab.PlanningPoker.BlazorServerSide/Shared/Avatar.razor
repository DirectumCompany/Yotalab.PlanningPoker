<MudAvatar Style="vertical-align:middle" Class="@this.ClassNames" Size="@this.imgSize">
  <MudImage Src="@this.imgSrc"></MudImage>
</MudAvatar>

@code {
  [Parameter]
  public string Url { get; set; }

  [Parameter]
  public string ClassNames { get; set; }

  [Parameter]
  public bool IsSmall { get; set; }

  [Parameter]
  public bool IsLarge { get; set; }

  private string imgSrc;
  private Size imgSize;

  protected override void OnParametersSet()
  {
    base.OnParametersSet();
    this.imgSrc = this.Url ?? AvatarStorage.DefaultAvatar;
    this.imgSize = this.IsLarge ? Size.Large : this.IsSmall ? Size.Small : Size.Medium;
  }
}

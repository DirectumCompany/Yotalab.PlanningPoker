@if (this.contextMenuItems.Count > 0)
{
  <DropDownMenu Items="this.contextMenuItems">
    <ButtonTemplate>
      <Avatar Url="@this.Participant.AvatarUrl" />
    </ButtonTemplate>
  </DropDownMenu>
}
else
{
  <Avatar Url="@this.Participant.AvatarUrl" />
}
